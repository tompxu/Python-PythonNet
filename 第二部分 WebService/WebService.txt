llib2:
    urllib2模块为实现不同协议的多个模块提供了一个通用的接口， 扩展性比urllib好
提交表单数据：
    GET方法把表单数据编码至URL。给出请求的页面，然后加上一个问号(?),接着是表单元素，键和值需要被"&"分隔，
    有些字符需要避免， URL中不能数据量太大
        在Post方法中URL是永远不会被修改的,附加信息通过urlopen的第二个参数传递
异常：
    HTTPError异常是一个文件类对象，为了得到错误文档可以像读取其他文件那样使用read()函数
    检查文件是否被删减需要根据服务器的回答找到内容长度的报头